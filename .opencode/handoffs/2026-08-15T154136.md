# Stage 1b Ship — Handoff for Stage 1c Kick-off

## Goal

Resume Stage 1c of the IELTS writing site (post Stage 1b ship). Stage 1b shipped 45 new essays + foundation work; awaiting user browser validation of Stage 1a + Stage 1b design + explicit kick-off of Stage 1c scope.

## Resolved

- **Stage 1a**: shipped 10 essays (5 Task 1 + 5 Task 2), live at https://meisijiya.github.io/IELTS/ (301→custom domain `xn--ljhfjm-dl0o.top` = `ljh爱fjm.top`).
- **Stage 1b**: shipped 45 new essays (8 Task 1 + 32 Task 2 first-essays + 5 Task 2 second-variants). All 14 spec scenarios PASS. Quick-review PASS on correctness / security / code quality. 52 commits since Stage 1a handoff `152a0dd`. HEAD = `e7169ad` on `origin/main`.
- **Stage 1b foundation**: `scripts/verify-stage1b.sh` (9 invariants + RED/GREEN self-test); `.github/workflows/deploy.yml` `cancel-in-progress: true`; 8 Task 2 chips in filter UI (5 from 1a + 3 new: `problem-solution`/`advantage-disadvantage`/`single-question`).
- **Stage 1b fix cycle**: T-064 bulk verify first FAIL (index missing 45 cards) → T-064a index extension → T-064b re-verify PASS (all 9 Task 2 chips populated).
- **HANDOFF documents**: `HANDOFF-stage1a.md` and `HANDOFF-stage1b.md` both committed at repo root.
- **Verified live**: 55/55 essay URLs return 200 (via custom domain); 9 Task 2 chips + 5 Task 1 chips populated (process=3, problem-solution=9, advantage-disadvantage=2, single-question=3, two-questions=2, opinion=1, agree-disagree=13, discuss-both-views=8, positive-negative=4).
- **`.omo/` gitignored**: `.omo/specs/`, `.omo/plans/`, `.omo/tickets/`, `.omo/boulder.json` live on disk only.

## Current state

- Working tree clean. HEAD = `e7169ad80b7b4c840c5f24ee08824310d69a5cad` matches `origin/main`.
- All Stage 1b essay commits (T-016 through T-064b + ship handoff) pushed.
- `bash scripts/verify-stage1b.sh docs/writing/` exits 0 on all 55 essays.
- User just said "推送" (push) — confirmed nothing more to push (0 unpushed commits).
- **Awaiting user browser validation** of Stage 1a + Stage 1b design at https://meisijiya.github.io/IELTS/writing/ (redirects to `ljh爱fjm.top`).

## Open

1. **User browser validation** of Stage 1b design: navigate `/IELTS/writing/` and `/IELTS/writing/task1/06-process-rain-shadow-desert.html` and similar essays. Confirm:
   - Filter chips work (click `[process]`, `[problem-solution]`, etc., see ≥1 essay).
   - Essay pages render with academic-minimal + reading-optimised palette.
   - Essay body 170-190 words (Task 1) or 270-290 words (Task 2).
2. **User picks Stage 1c scope** from three candidates (see HANDOFF-stage1b.md §"Next stage (Stage 1c)"):
   - (A) **Multi-prompt coverage** for 32 already-covered sub-cats (~32 more essays, each sub-cat × second chip).
   - (B) **Drop-out chip fillers** — re-balance chip distribution (currently skewed: agree-disagree=13, problem-solution=9; opinion=1).
   - (C) **Visual polish** — run visual-qa dual-oracle review on the 55-essay corpus, fix any design-system issues found.
3. **Stage 1c plan** after scope is picked: spec-gate → Prometheus plan → Momus review → to-tickets → /start-work → bulk verify → ship-evidence-gate → /review-work (5 lanes) → fix → HANDOFF-stage1c.md.

## Constraints

- **HTTPS custom domain** out of repo scope (user-level GitHub Pages setting `https_enforced: false` on `xn--ljhfjm-dl0o.top`). Do not attempt to change.
- **Chart data must NOT be fabricated** — Task 1 essays must trace every numeric figure to the source PNG via `look_at`. BLOCKED if unreadable, no guessing.
- **9 template invariants** (from Stage 1a review → Stage 1b preserved): exactly one `<h1>`, one `<main>`, `<article data-task data-difficulty data-type>` on same line, 5-10 `<code>` keywords, Task 1 `<img loading="lazy" decoding="async" alt width height>` + `<figcaption>`, Task 1 word count 170-190, Task 2 word count 270-290, `data-type` from 8-chip whitelist, no `<figure>` for Task 2.
- **Always extend `docs/writing/index.html` with new `<article>` cards** in the same commit(s) as the essay files, OR in a follow-up "T-NNNa extend-index" commit. Cheap, ~1 min via Python script reading `<article>` + `<h1>` from each new essay.
- **`7.1 音乐` sub-category dropped** from Stage 1b (out of 32-first-essay budget). Out of scope unless user requests.
- **Commit message format**: `stage 1c(T-NNN): <slug> — <one-line what>`.
- **GitHub Actions SHA-pinned** (not tag).
- **Run `bash scripts/verify-stage1b.sh docs/writing/` after every batch of essay commits** to catch regressions early.

## Suggested skills

In order, for Stage 1c kick-off after user picks scope:

1. **`using`** — OMO stack + 8-stage Phase Gate Chain + 22 built-in skills + 5 commands overview.
2. **`grill-me`** — adversarial 5-dimension question loop on Stage 1c scope (assumptions / boundaries / failure modes / counter-examples / reversibility) before writing spec.
3. **`spec-gate`** — write `.omo/specs/ielts-writing-site-stage1c.md` EXPLORED (≥3 Requirements, each with ≥1 Scenario).
4. **`/ulw-plan`** — Prometheus plan in `.omo/plans/stage1c.md` (parallel fan-out graph + per-ticket AC + risks + rollback).
5. **Momus review** — Momus must PASS the plan before tickets.
6. **`to-tickets`** — generate `.omo/tickets/ielts-writing-site-stage1c/` (one ticket per essay / foundation item).
7. **`/start-work`** — execute tickets in wave order (Wave 0 foundation → parallel essay waves → bulk verify → ship).

Do NOT load `ponytail`, `tdd`, `ui-ux-pro-max`, `design-system`, `frontend` (not relevant for HTML-content-only Stage 1c additions). Do load `git-master` for atomic commits.

## Context summary

Stage 1b of the IELTS writing site is fully shipped: 55 essays (13 Task 1 + 42 Task 2) live on GitHub Pages at https://meisijiya.github.io/IELTS/, all 14 spec acceptance scenarios PASS, quick-review (correctness + security + code quality) PASS, 52 commits on `main` since Stage 1a. One known limitation: HTTP-only custom domain (`xn--ljhfjm-dl0o.top`) inherited from Stage 1a, HTTPS upgrade out of repo scope. Stage 1b shipped 45 new essays, a bulk-verify script (`scripts/verify-stage1b.sh`), a workflow tweak (`cancel-in-progress: true`), and an index extension (45 new `<article>` cards). The current session is at a stable boundary: waiting for user browser validation of Stage 1b design and an explicit kick-off of Stage 1c scope (multi-prompt coverage / drop-out chip fillers / visual polish). All artifacts on disk; nothing in flight.

## References (paths only — do not `@file`-embed)

- Working dir: `/home/ljh2923/opencode-project/IELTS`
- Stage 1b handoff: `/home/ljh2923/opencode-project/IELTS/HANDOFF-stage1b.md`
- Stage 1a handoff: `/home/ljh2923/opencode-project/IELTS/HANDOFF-stage1a.md`
- Stage 1b spec: `/home/ljh2923/opencode-project/IELTS/.omo/specs/ielts-writing-site-stage1b.md`
- Stage 1b plan: `/home/ljh2923/opencode-project/IELTS/.omo/plans/stage1b.md`
- Stage 1b tickets: `/home/ljh2923/opencode-project/IELTS/.omo/tickets/ielts-writing-site-stage1b/`
- Stage 1b boulder: `/home/ljh2923/opencode-project/IELTS/.omo/boulder.json`
- Source docx: `/home/ljh2923/opencode-project/IELTS/Task 1 冲刺(1).docx`, `/home/ljh2923/opencode-project/IELTS/作文真题储备（近五年）_可修改.docx`
- Verify script: `/home/ljh2923/opencode-project/IELTS/scripts/verify-stage1b.sh`
- Workflow: `/home/ljh2923/opencode-project/IELTS/.github/workflows/deploy.yml`
- Index: `/home/ljh2923/opencode-project/IELTS/docs/writing/index.html`
- Essays: `/home/ljh2923/opencode-project/IELTS/docs/writing/task{1,2}/*.html`
- Chart PNGs: `/home/ljh2923/opencode-project/IELTS/docs/assets/images/task1-charts/*.png`
- Screenshots: `/home/ljh2923/opencode-project/IELTS/docs/screenshots/*.png`
- Assertions log: `/home/ljh2923/opencode-project/IELTS/docs/screenshots/_assertions-stage1b.log`

## Continue-prompt for next session

Copy-paste this into a new session:

```
Read /home/ljh2923/opencode-project/IELTS/.opencode/handoffs/2026-08-15T154136.md and continue from where the previous session stopped.

Context: Stage 1b of the IELTS writing site is fully shipped — 55 essays live on GitHub Pages, all 14 spec scenarios PASS, 52 commits on main, HEAD e7169ad. User is at a stage boundary: just confirmed all Stage 1b content is pushed; awaiting their browser validation of Stage 1b design at https://meisijiya.github.io/IELTS/ (redirects to ljh爱fjm.top custom domain) and an explicit kick-off decision on Stage 1c scope.

First action: load `using` skill for OMO 8-stage Phase Gate Chain overview, then wait for the user's next message (which will be either Stage 1b validation feedback OR Stage 1c scope pick). Do not start Stage 1c spec / plan / tickets until the user explicitly picks a scope from HANDOFF-stage1b.md §"Next stage (Stage 1c) — recommended approach" (multi-prompt coverage / drop-out chip fillers / visual polish) or proposes a different one. If the user asks for a quick check first (e.g., re-verify a chip filter live), use `bash scripts/verify-stage1b.sh docs/writing/` and `curl -sIL http://xn--ljhfjm-dl0o.top/IELTS/...` for diagnostics.
```