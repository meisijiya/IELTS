---
id: T-060
goal: Task 2 second-prompt-variant for sub-category 1.1 using the new chip `problem-solution` — fills the chip filter's empty-type state for this sub-category.
files:
  - docs/writing/task2/39-1-1-second-variant-second-variant.html
deps: [T-018]
ac:
  - REQ-task2-batch-coverage-scenario-task2-5-repeat-subcat-prompts-shipped
  - REQ-template-invariants-scenario-word-count-band
  - REQ-template-invariants-scenario-single-h1-single-main
  - REQ-template-invariants-scenario-article-data-attrs-present
  - REQ-template-invariants-scenario-keyword-list-5-to-10
evidence: file exists at docs/writing/task2/39-1-1-second-variant-second-variant.html; bash scripts/verify-stage1b.sh exits 0; `data-type="problem-solution"` differs from the Stage 1a sibling essay's `data-type` for sub-category 1.1; word count in [270, 290].
size: S
status: ready-for-agent
created: 2026-08-15
feature: ielts-writing-site-stage1b
---

## What to build

Second-prompt-variant Task 2 essay for sub-category **1.1** (theme: education) from `作文真题储备（近五年）_可修改.docx`. The Stage 1a sibling essay already exists for this sub-category with a specific `data-type` chip; this second variant must use a **different** chip — specifically `problem-solution` (one of the 3 new chips added in T-016b).

Subagent steps:

1. Look up the Stage 1a sibling essay for sub-category 1.1 in `docs/writing/task2/` to confirm its `data-type` chip.
2. Pick a **different** prompt from the same sub-category in `作文真题储备` (a different numbered `#N`) so the second variant's content is genuinely different.
3. Compose essay using `data-type="problem-solution"` (the new chip from T-016b's whitelist).
4. Draft 270–290 word 6-band-clean essay.
5. Write Chinese 1–2 paragraph rubric + 5–10 `<code>` keyword list.
6. Compose HTML copying the Stage 1a sibling as skeleton (or `01-agree-disagree-history-vs-business.html`); swap content + `data-type`.
7. Self-run `bash scripts/verify-stage1b.sh docs/writing/task2/39-1-1-second-variant-second-variant.html` → must exit 0.
8. Commit + push.

## Acceptance criteria

- [ ] File `docs/writing/task2/39-1-1-second-variant-second-variant.html` exists.
- [ ] `<article>` carries `data-task="task2"`, `data-difficulty` (writer's choice), **`data-type="problem-solution"`** on same line.
- [ ] **`data-type="problem-solution"` ≠ Stage 1a sibling's `data-type`** (AC-T2-4).
- [ ] Word count in [270, 290].
- [ ] Verify script exits 0.

## Verification

- [ ] `bash scripts/verify-stage1b.sh docs/writing/task2/39-1-1-second-variant-second-variant.html` exits 0.
- [ ] `grep 'data-type="problem-solution"' docs/writing/task2/39-1-1-second-variant-second-variant.html` returns 1.
- [ ] After T-016b deploy: live filter shows this essay when chip `problem-solution` is clicked.

## Files in scope

- `docs/writing/task2/39-1-1-second-variant-second-variant.html` (create).

## Files out of scope

- Stage 1a sibling essay (untouched).
- Other Task 2 essay pages (siblings).
- The Writing index (T-016b's domain — chip already added there).
- Task 2 docx (read-only).

## BLOCKED condition

If sub-category 1.1 has only one numbered prompt in `作文真题储备` (no alternative available for second-variant), pick a sub-adjacent sub-category and escalate to dispatcher. Do NOT reuse the Stage 1a prompt's wording verbatim.
